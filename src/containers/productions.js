import React from 'react';
import { Productions } from '../components';

const ProductionsContainer = ({ children }) => {
    return (
        <Productions>
            {children}
        </Productions>
    )
}

export default ProductionsContainer;
